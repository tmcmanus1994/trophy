/**
 * Site owner identity, shown on the library home.
 * Drop the avatar image at public/profile/avatar.png — if the file is
 * missing the chip falls back to initials automatically.
 */
export const profile = {
  psnId: "BlackXThunder25",
  avatar: "/profile/avatar.png",
  initials: "BT",
};
